import ComingSoon from "@/components/ui/ComingSoon";

export const metadata = { title: "Account" };

export default function Page() {
  return (
    <ComingSoon
      eyebrow="Members"
      title="Your Account"
      body="Register, sign in, review your order history and manage saved details. Accounts arrive in the next pass."
    />
  );
}
